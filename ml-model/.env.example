# ThermalAI ML Flask API — environment template

# Port the Flask app listens on. Docker + Render set this externally.
PORT=5001

# CORS allow-list. Comma-separated origins accepted by Flask CORS.
# Never use "*" in production.
FRONTEND_ORIGIN=http://localhost:3000
